spring:
  datasource:
    url: jdbc:h2:mem:discount_web_app;DB_CLOSE_DELAY=-1;
    driver-class-name: org.h2.Driver
    hikari:
      maximum-pool-size: 5
      minimum-idle: 2
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  liquibase:
    enabled: false
